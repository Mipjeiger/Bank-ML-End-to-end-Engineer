apiVersion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata:
  name: banking-llm
spec:
  predictors:
  - name: default
    replicas: 1
    graph:
      name: banking-llm
      implementation: UNKNOWN
      endpoint:
        type: REST
    componentSpecs:
    - spec:
        containers:
        - name: banking-llm
          image: your-dockerhub/banking-llm:latest